import { TicketWizard } from '@/features/tickets/components/wizard/TicketWizard'

// Wizard is a standalone page — no bottom nav, no layout padding
export default function NewTicketPage() {
  return <TicketWizard />
}
